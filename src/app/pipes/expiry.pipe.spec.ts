/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ExpiryPipe } from './expiry.pipe';

describe('Pipe: Expiry', () => {
  it('create an instance', () => {
    let pipe = new ExpiryPipe();
    expect(pipe).toBeTruthy();
  });
});
